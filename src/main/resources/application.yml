spring:
  thymeleaf:
    mode: HTML

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring_boot_advance?useUnicode=true&characterEncoding=utf-8
    username: root
    password: 572641339
  jpa:
    hibernate:
      ddl-auto: update
    show-spl: true

logging:
  level:
    root: info
    com.lrm: debug
  file: log/my.log

